#
# Module list consists of module names and their enable status
#
# template: ('<module file name without .py>', <True or False, depending if you want to load them or not>)
# ie.       ('thermometer', True)
#

module_list = [
	("mod1", True),
	("mod2", True),
	("srm", True)
]
